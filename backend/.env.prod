PORT=3000
NODE_ENV=production

# Production PostgreSQL Database configuration (direct connection)
# No SSH tunnel needed - direct database access
DB_HOST=localhost
DB_PORT=5432
DB_NAME=subsync
DB_USER=sathish
DB_PASSWORD=Sathish@09

# JWT Secret (use strong secret in production)
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# API Configuration
API_VERSION=v1