PORT=3000
NODE_ENV=development

# Local PostgreSQL Database configuration via SSH tunnel
# SSH tunnel: ssh -f -N -L 5432:localhost:5432 sathish@192.168.1.2
DB_HOST=localhost
DB_PORT=5432
DB_NAME=subsync
DB_USER=sathish
DB_PASSWORD=Sathish@09

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# API Configuration
API_VERSION=v1